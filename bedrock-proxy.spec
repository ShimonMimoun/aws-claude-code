# PyInstaller spec pour l'exécutable bedrock-proxy
# Build : pyinstaller bedrock-proxy.spec
# Résultat : dist/bedrock-proxy/bedrock-proxy (ou .exe sous Windows)

block_cipher = None

a = Analysis(
    ['bedrock_proxy_main.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[
        'uvicorn.logging',
        'uvicorn.loops',
        'uvicorn.loops.auto',
        'uvicorn.protocols',
        'uvicorn.protocols.http',
        'uvicorn.protocols.http.auto',
        'uvicorn.protocols.websockets',
        'uvicorn.protocols.websockets.auto',
        'uvicorn.lifespan',
        'uvicorn.lifespan.on',
        'fastapi',
        'starlette',
        'starlette.routing',
        'starlette.middleware',
        'pydantic',
        'pydantic_settings',
        'boto3',
        'botocore',
        'jose',
        'jose.jwt',
        'msal',
        'httpx',
        'config',
        'src',
        'src.main',
        'src.client',
        'src.auth',
        'src.auth.dependencies',
        'src.auth.entra',
        'src.auth.cognito',
        'src.auth.models',
        'src.aws',
        'src.aws.credentials',
        'src.aws.proxy',
        'src.usage',
        'src.usage.store',
        'src.usage.models',
        'src.login_web',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='bedrock-proxy',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
